{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test":"lerna run test",
    "preversion": "lerna run preversion",
    "postversion": "lerna run postversion"
  }
}
